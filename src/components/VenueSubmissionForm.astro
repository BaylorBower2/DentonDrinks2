---
---
<form 
  action="/api/submit-venue" 
  method="POST" 
  class="max-w-2xl mx-auto bg-dark-card p-6 rounded-lg shadow-lg border border-dark-border mt-12"
>
  <h2 class="text-2xl font-bold mb-6 text-secondary">Submit a New Venue</h2>
  
  <div class="space-y-4">
    <div>
      <label for="name" class="block text-sm font-medium text-gray-300">Venue Name</label>
      <input
        type="text"
        id="name"
        name="name"
        required
        class="mt-1 block w-full rounded-md bg-dark-surface border-dark-border text-gray-100 shadow-sm focus:border-secondary focus:ring-secondary"
      />
    </div>

    <div>
      <label for="type" class="block text-sm font-medium text-gray-300">Type</label>
      <select
        id="type"
        name="type"
        required
        class="mt-1 block w-full rounded-md bg-dark-surface border-dark-border text-gray-100 shadow-sm focus:border-secondary focus:ring-secondary"
      >
        <option value="bar">Bar</option>
        <option value="brewery">Brewery</option>
      </select>
    </div>

    <div>
      <label for="address" class="block text-sm font-medium text-gray-300">Address</label>
      <input
        type="text"
        id="address"
        name="address"
        required
        class="mt-1 block w-full rounded-md bg-dark-surface border-dark-border text-gray-100 shadow-sm focus:border-secondary focus:ring-secondary"
      />
    </div>

    <div>
      <label for="description" class="block text-sm font-medium text-gray-300">Description</label>
      <textarea
        id="description"
        name="description"
        rows="4"
        required
        class="mt-1 block w-full rounded-md bg-dark-surface border-dark-border text-gray-100 shadow-sm focus:border-secondary focus:ring-secondary"
      ></textarea>
    </div>

    <div>
      <label for="submitterEmail" class="block text-sm font-medium text-gray-300">Your Email</label>
      <input
        type="email"
        id="submitterEmail"
        name="submitterEmail"
        required
        class="mt-1 block w-full rounded-md bg-dark-surface border-dark-border text-gray-100 shadow-sm focus:border-secondary focus:ring-secondary"
      />
    </div>

    <button
      type="submit"
      class="w-full bg-secondary hover:bg-secondary-light text-white px-4 py-2 rounded-md transition-colors focus:outline-none focus:ring-2 focus:ring-secondary focus:ring-offset-2 focus:ring-offset-dark-bg"
    >
      Submit Venue
    </button>
  </div>
</form>